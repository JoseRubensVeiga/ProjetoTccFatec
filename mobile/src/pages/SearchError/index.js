import React from 'react';
import {Text} from 'react-native';

// import { Container } from './styles';

const SearchError = () => {
  return <Text>SearchError</Text>;
};

export default SearchError;
